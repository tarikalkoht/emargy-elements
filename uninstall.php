<?php
/**
 * Uninstall file for Emargy Elements plugin
 *
 * @package EmargyElements
 */

// Exit if accessed directly.
if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
    exit;
}

// Delete plugin options if needed
// delete_option( 'emargy_elements_options' );